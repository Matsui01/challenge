name: matsui
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3
  mobx: ^2.0.4
  flutter_mobx: ^2.0.2
  

dev_dependencies:
  # flutter_test:
  #   sdk: flutter
  mobx_codegen: ^2.0.2
  build_runner: ^2.1.1
  flutter_launcher_icons: ^0.9.2

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/logo_pequeno.png"
  adaptive_icon_background: "#222222"
  adaptive_icon_foreground: "assets/logo_pequeno.png"
  image_path_ios: "assets/logo_pequeno.png"
  
flutter:
  assets:
    - assets/
  uses-material-design: true

  